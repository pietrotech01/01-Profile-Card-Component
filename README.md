# Frontend Mentor - Profile card component

## What I learned

- I need to have overflow hidden on the parent element so the border radius shows.
- Use position: absolute so objects don't impact other elements especially flex items
- set root container to min-height 100vh so align items works

![snapshot](profile-card-component.png)
